# Optional HUB devcontainer bootstrap. Production does not depend on this image.
ARG HUB_DEVCONTAINER_IMAGE=ghcr.io/wkarts/argws-connect-hub:develop
FROM ${HUB_DEVCONTAINER_IMAGE}

USER root
WORKDIR /workspace
CMD ["sleep", "infinity"]
