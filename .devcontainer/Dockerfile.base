# Optional HUB devcontainer bootstrap.
# Publish HUB itself first; this image is not required for production.
ARG HUB_DEVCONTAINER_IMAGE=ghcr.io/wkarts/hub:latest
FROM ${HUB_DEVCONTAINER_IMAGE}
USER root
WORKDIR /workspace
CMD ["sleep", "infinity"]
