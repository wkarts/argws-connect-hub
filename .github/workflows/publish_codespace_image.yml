name: Publish HUB Codespace AMD64

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: HUB audit
        run: ./scripts/audit-hub.sh
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/Dockerfile
          platforms: linux/amd64
          push: true
          build-args: |
            HUB_DEVCONTAINER_IMAGE=ghcr.io/${{ github.repository_owner }}/hub:latest
          tags: ghcr.io/${{ github.repository_owner }}/hub-codespace:latest
